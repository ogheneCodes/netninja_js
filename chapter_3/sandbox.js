//for loops
for(let i = 0; i < 5; i++){
    document.write('i in for loop is less than:', i + "<br><br>")
}
const name = ['max', 'sixtus', 'chibike', 'Terry'];
 for(e=0; e <name.length; e++){
    let html = `<div>${name[e]}</div>`
    document.write(html);
 }